import React, { useEffect } from 'react';
import io from 'socket.io-client';

// const socket = io.connect('http://localhost:80');


const Chat = (props) => {
  useEffect(() => {

  }, []);

  return (
    <div>
      HEYYY THIS IS THE CHAT
    </div>
  );
}

export default Chat;
